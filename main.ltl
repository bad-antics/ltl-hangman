// ltl-hangman/main.ltl
//
// Hangman with word-list frequency analysis.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-hangman v0.1.0")
    io::println("Hangman with word-list frequency analysis.")
    0
}
