// examples/demo.ltl — runnable demo for ltl_hangman

import std::io
use ltl_hangman as lib

fn main() {
    io::println("ltl_hangman demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}